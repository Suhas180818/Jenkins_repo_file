pipeline{
    agent any 

    stages{
        stage('awsAccess'){
            steps {
                sh'''
                    aws --version
                    credentials=$(aws sts get-caller-identity)
                    echo "$credentials"
                '''
            }
        }
    }
}
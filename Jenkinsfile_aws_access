pipeline{
    agent any 

    stages{
        stage('awsAccess'){
            steps {
                sh'''
                    credentials=$(/usr/local/bin/aws sts get-caller-identity)
                    echo "$credentials"
                '''
            }
        }
    }
}
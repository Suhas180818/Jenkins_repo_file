pipeline{
    agent any 

    stages{
        stage('awsAccess'){
            steps {
                sh'''
                    aws --version
                    aws sts get-caller-identity
                    
                '''
            }
        }
    }
}
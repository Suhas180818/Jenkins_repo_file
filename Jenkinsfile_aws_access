pipeline{
    agent any 

    stages{
        stage('awsAccess'){
            steps {
                sh'''
                    credentials=$(aws sts get-caller-identity)

                    echo "$credentials"
                
                '''
            }
        }
    }
}
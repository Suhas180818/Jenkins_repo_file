pipeline {

    agent none

    stages{
        stage('Build') {
            
            agent {label 'ubuntulinux01'}
            
            steps {
                echo "This is the bulid"
                sh '''
                    echo "This the shell executed Command in Stage 1"              
                '''
            }
        }
        stage('Test') {
            
            agent {label 'ubuntulinux02'}
            
            steps {
                sh '''
                    #!/bin/bash
                    ls -lrt
                    echo "Shell Command Executing in Stage2"
                    sleep 10 
                ''' 

            }
        }
        stage('Deploy') {

            agent {label 'ubuntulinux01'}

            steps {
                sh '''
                    #!/bin/bash
                    ls -lrt
                    echo "Shell Command Deploying the config file"
                    sleep 5
                ''' 

            }
        }
    }   
}